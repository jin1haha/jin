#!/data/data/com.termux/files/usr/bin/bash
# Install Termux:Boot (from F-Droid, same source as Termux itself), then
# copy this file to ~/.termux/boot/boot-start-jin.sh and make it executable.
# It will run automatically every time the phone reboots, so JIN's server
# survives restarts without you opening Termux by hand.

~/mysched/termux/start.sh &
